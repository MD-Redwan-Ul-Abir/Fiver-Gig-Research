#!/bin/bash
# Double-click this file to open the Fiverr Research Hub.
# (Right-click → Open the first time if macOS shows a security warning)

DIR="$(cd "$(dirname "$0")" && pwd)"
python3 "$DIR/FiverrResearchHub.py"
